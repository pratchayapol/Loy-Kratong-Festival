docker compose up -d

// สร้าง table
docker compose exec app php artisan make:migration create_registers_table --create=registers

// กำหนด scema
Schema::create('registers', function (Blueprint $table) {
    $table->id();
    $table->string('name');
    $table->string('email')->unique();
    $table->timestamps();
});

docker compose exec app php artisan migrate --force
docker compose exec app php artisan make:migration alter_users_add_google_columns --table=users
composer require barryvdh/laravel-dompdf

docker compose exec app composer dump-autoload
docker compose exec app php artisan route:clear
docker compose exec app php artisan config:clear
docker compose exec app php artisan cache:clear
docker compose exec app php artisan optimize:clear
docker compose exec app php artisan route:list | grep admin.home

//ทดลองส่งเมล นำเมล็ดออกปลูก
docker compose exec app php artisan queue:work --once